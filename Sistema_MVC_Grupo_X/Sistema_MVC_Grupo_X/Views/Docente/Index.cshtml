@model List<Sistema_MVC_Grupo_X.Models.Docente>
@{
    ViewBag.Title = "Index";
}

<h2 class="card text-center">Gestión de Semestres</h2>
<br />
<div class="text-right">
    <a href="~/Semestre/AgregarEditar/" class="btn btn-secondary text-right">
        Nuevo Registro
    </a>
</div>
<br />
<table class="table">
    <thead>
        <tr>
            <th style="width:50px">Id</th>
            <th style="width:100px">Docente</th>
            <th style="width:70px">Email - Celular</th>
            <th style="width:80px">Estado</th>
            <th style="width:200px">Acciones</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var d in Model)
        {
            <tr>
                <th>@(d.docente_id)</th>
                <th>@(d.nombre + " " + d.apellido)</th>
                <th>@(d.email + " / " + d.celular)</th>
                <th>@(d.estado == "A" ? "Activo" : "Inactivo")</th>
                <th style="text-align:center; align-content:center">
                    <a href="~/Semestre/Visualizar/@d.docente_id"
                       class="btn btn-xs btn-warning">Visualizar</a>
                    <a href="~/Semestre/AgregarEditar/@d.docente_id"
                       class="btn btn-xs btn-primary">Modificar</a>
                    <a href="~/Semestre/Eliminar/@d.docente_id"
                       class="btn btn-xs btn-danger"
                       onclick="return confirm('¿Esta seguro de eliminar ...?');">Eliminar</a>
                </th>


            </tr>
        }
    </tbody>
</table>